# subtitle_gen.py マニュアル

音楽ファイルと歌詞テキストから、YouTube用の字幕ファイル（SRT形式）を自動生成するツールです。

---

## 目次

1. [必要環境](#必要環境)
2. [インストール](#インストール)
3. [基本的な使い方](#基本的な使い方)
4. [オプション一覧](#オプション一覧)
5. [歌詞ファイルの書き方](#歌詞ファイルの書き方)
6. [出力ファイル（SRT形式）について](#出力ファイルsrt形式について)
7. [YouTubeへのアップロード方法](#youtubeへのアップロード方法)
8. [モデルの選び方](#モデルの選び方)
9. [よくある質問](#よくある質問)
10. [トラブルシューティング](#トラブルシューティング)

---

## 必要環境

- Python 3.10 以上
- ffmpeg（音声処理に必要）
- インターネット接続（初回実行時にWhisperモデルをダウンロード）

---

## インストール

### 1. ffmpeg のインストール

**macOS**
```bash
brew install ffmpeg
```

**Ubuntu / Debian**
```bash
sudo apt install ffmpeg
```

**Windows**
[ffmpeg公式サイト](https://ffmpeg.org/download.html) からダウンロードし、パスを通してください。

### 2. Pythonパッケージのインストール

```bash
pip install -r requirements.txt
```

---

## 基本的な使い方

```bash
python subtitle_gen.py <音楽ファイル> <歌詞ファイル> [オプション]
```

**最もシンプルな実行例**

```bash
python subtitle_gen.py song.mp3 lyrics.txt
```

→ `song.srt` が同じフォルダに生成されます。

**日本語の曲（精度重視）**

```bash
python subtitle_gen.py song.mp3 lyrics.txt --language ja --model large-v3
```

**出力先ファイルを指定**

```bash
python subtitle_gen.py song.mp3 lyrics.txt -o subtitles/output.srt
```

---

## オプション一覧

| オプション | 短縮形 | デフォルト | 説明 |
|-----------|--------|-----------|------|
| `--output <ファイル>` | `-o` | `<音楽ファイル名>.srt` | 出力SRTファイルのパス |
| `--model <モデル名>` | — | `base` | 使用するWhisperモデル（後述） |
| `--language <言語コード>` | `-l` | 自動検出 | 言語を指定（`ja`, `en`, `ko` など） |

**使用例**

```bash
# 出力先・言語・モデルをすべて指定
python subtitle_gen.py song.mp3 lyrics.txt -o out.srt -l ja --model medium

# ヘルプを表示
python subtitle_gen.py --help
```

---

## 歌詞ファイルの書き方

- **1行 = 1字幕エントリ**
- `#` で始まる行はコメントとして無視されます
- `[Verse]` `[Chorus]` などSunoのタグ行（`[` と `]` で囲まれた行）は無視されます
- 空行は無視されます

**例: `lyrics.txt`**

```
# イントロはなし
[Verse]
さくらさくら
やよいの空は
見渡す限り
かすみか雲か
匂いぞ出ずる
いざや いざや
見にゆかん
```

**注意事項**
- 文字コードは UTF-8 で保存してください
- 1行が長すぎると字幕が読みにくくなります。画面に収まる長さ（目安：全角20文字程度）に分割することを推奨します
- 間奏など歌詞のない区間は、歌詞ファイルに記述しなくてOKです（その間は字幕が表示されません）

---

## 出力ファイル（SRT形式）について

生成されるSRTファイルは以下の形式です。

```
1
00:00:02,340 --> 00:00:04,120
さくらさくら

2
00:00:04,200 --> 00:00:06,050
やよいの空は

3
00:00:06,100 --> 00:00:08,300
見渡す限り
```

- **番号**: 字幕の連番
- **タイムコード**: `時:分:秒,ミリ秒` の形式
- YouTube、動画編集ソフト（DaVinci Resolve、Premiereなど）で利用可能

---

## YouTubeへのアップロード方法

1. YouTube Studio（[studio.youtube.com](https://studio.youtube.com)）を開く
2. 該当動画の **「字幕」** メニューを選択
3. **「字幕を追加」** → **「ファイルをアップロード」** を選択
4. ファイルの種類で **「タイミング付き字幕ファイル」** を選ぶ
5. 生成した `.srt` ファイルをアップロード
6. 内容を確認して **「保存」**

---

## モデルの選び方

初回実行時にモデルが自動ダウンロードされます（要インターネット接続）。

| モデル名 | ダウンロードサイズ | 処理速度 | 精度 | 推奨用途 |
|---------|------------|------|------|---------|
| `tiny` | 約 75 MB | 最速 | 低 | 動作確認 |
| `base` | 約 145 MB | 速い | やや低 | テスト（デフォルト） |
| `small` | 約 465 MB | 中程度 | 中 | 英語の曲 |
| `medium` | 約 1.5 GB | やや遅い | 高 | 日本語・中国語 |
| `large-v3` | 約 3.1 GB | 遅い | 最高 | 日本語・精度最優先 |

**推奨**
- 日本語の曲 → `medium` または `large-v3`
- 英語の曲 → `small` または `medium`
- まず試したい → `base`（デフォルト）

---

## よくある質問

**Q. MP3とWAV、どちらが精度が良いですか？**  
A. 精度に大きな差はありません。ただし、音質が高い（ノイズが少ない）ファイルほどアライメント精度が上がります。

**Q. 字幕の数が歌詞の行数と合わない場合は？**  
A. 一部の行がアライメントに失敗すると結合または欠落することがあります。より大きいモデル（`medium` や `large-v3`）を試してください。また `--language` で言語を明示すると改善することがあります。

**Q. 間奏中に字幕が表示されてしまいます**  
A. 歌詞ファイルに間奏部分の行がある場合は削除してください。

**Q. 処理時間の目安は？**  
A. 曲の長さとモデルサイズによります。例として5分の曲でのおおよその目安（M1 Mac）：

| モデル | 処理時間の目安 |
|--------|-------------|
| `base` | 約1〜2分 |
| `medium` | 約5〜8分 |
| `large-v3` | 約10〜15分 |

---

## トラブルシューティング

**`stable-whisper not found` エラー**
```bash
pip install stable-whisper
```

**`ffmpeg not found` エラー**  
ffmpegがインストールされていないか、パスが通っていません。[インストール手順](#1-ffmpeg-のインストール)を参照してください。

**`Error: Audio file not found`**  
音楽ファイルのパスが正しいか確認してください。スペースを含むパスはクォートで囲んでください。
```bash
python subtitle_gen.py "my song.mp3" lyrics.txt
```

**`Error: No lyrics found in file`**  
歌詞ファイルが空か、すべての行が `#` で始まっています。内容を確認してください。

**字幕のタイミングがズレる**  
- `--model` を `medium` または `large-v3` に変更する
- `--language` で言語コードを明示する（例: `--language ja`）
- 歌詞ファイルの行区切りが実際の歌唱フレーズと一致しているか確認する
